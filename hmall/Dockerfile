git clone git@github.com:YFEYI/dockerimages.gitmkdir -vp dockerimages/hmall << EOF
FROM openjdk:11.0-jre-buster
LABEL MAINTAINER=master@yfeyi.top BUILDTYPE=Aliyun
EOF git add . && git commit -m "hmall" && git push
